{
  "name": "dsh-cloud-sync",
  "description": "E2E-encrypted cloud session sync plugin for deepseek-harness",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
